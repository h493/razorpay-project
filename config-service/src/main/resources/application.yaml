spring:
  application:
    name: config-service

  cloud:
    config.server.git:
      uri: https://github.com/h493/distributed-razorpay-config
      username: ${CONFIG_GIT_USERNAME:}
      password: ${CONFIG_GIT_PASSWORD:}
      default-label: main

server.port: 8888


