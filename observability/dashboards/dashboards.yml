apiVersion: 1

providers:
  - name: razorpay-dashboards
    folder: Razorpay
    type: file
    options:
      # same directory this file is mounted into; Grafana loads every *.json dashboard in it
      path: /etc/grafana/provisioning/dashboards
